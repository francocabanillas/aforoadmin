import React from 'react';
import { render } from '@testing-library/react';

import NuevoProyecto from '../components/layouts/NuevoProyecto'

test('<NuevoProyecto /> Cargar el formulario y probar que todo este correcto', () => {

    const wrapper = render(<NuevoProyecto />)

})